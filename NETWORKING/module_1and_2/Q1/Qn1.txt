The cp command can be used if you want to copy a folder and all its contents to another directory on the same machine
If you want to copy a folder to another machine over a network (SSH-based transfer):scp command can be used
